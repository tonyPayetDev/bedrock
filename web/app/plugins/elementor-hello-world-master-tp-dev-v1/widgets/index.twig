
{% block content %}
    <div id="app">Chargement</div>
    {# <button type="button" id="react" class="btn btn-primary" style="background-color:{{params.color}}">toto</button> #}
    <pre>
{# {{ dump(user.user_url) }} #}
     </pre>

{% endblock %}