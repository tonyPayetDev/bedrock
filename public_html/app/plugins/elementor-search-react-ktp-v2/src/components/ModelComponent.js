import React from "react";

// un model a copier coller pour commencer un component
const Model = (props) => {
  const { name } = props;
  console.log(name);
  return (
    <div>

    </div>
  );
};

export default Model;
